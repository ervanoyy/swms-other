import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DaftarlaporPage } from './daftarlapor';

@NgModule({
  declarations: [
    DaftarlaporPage,
  ],
  imports: [
    IonicPageModule.forChild(DaftarlaporPage),
  ],
})
export class DaftarlaporPageModule {}
